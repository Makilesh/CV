# CPU-only dependency set for the containerised replay/eval path.
#
# Deliberately NOT the same as the root requirements.txt: no CUDA torch, no onnxruntime-gpu, no
# timm, no transformers/bitsandbytes. Those exist to produce the measured numbers on the specific
# machine described in RESULTS.md, and this container must not appear to reproduce them.
#
# Install torch from the CPU index:
#   pip install -r requirements-eval.txt --extra-index-url https://download.pytorch.org/whl/cpu
numpy==2.5.1
opencv-python-headless==5.0.0.93
hydra-core==1.3.4
omegaconf==2.3.1
matplotlib==3.11.1
pytest==9.1.1

# The eval path imports torch only to record environment metadata and to build ONNX encoders when
# they are present. The CPU build is enough; the fast-tier encoder falls back to `downsample`
# in this container (configs/fasttier/ci.yaml).
torch==2.11.0
